# =========================================================
# GLOBAL SETTINGS
# =========================================================
microservice:
  nameOverride: notification-service
  replicaCount: 1

  # =========================================================
  # CONTAINER IMAGE SETTINGS
  # =========================================================
  image:
    repository: notification-service
    tag: v1
    pullPolicy: IfNotPresent

  # Override default CMD to load only minikube config
  args:
    - "-conf"
    - "./configs/config-minikube.yaml"

  # =========================================================
  # ENVIRONMENT VARIABLES (ConfigMap)
  # =========================================================
  envVarsCM:
    GO_ENV: development
    # MongoDB Configuration - database name only (URI in secret)
    MONGODB_DATABASE: "notification_db"
    # Redis Configuration - non-sensitive settings
    REDIS_HOST: "redis-cluster.redis.svc.cluster.local"
    REDIS_PORT: "6379"
    # OpenTelemetry Configuration
    OTEL_EXPORTER_OTLP_ENDPOINT: "otel-collector.tracing.svc.cluster.local:4317"
    OTEL_SERVICE_NAME: "notification-service"
    # Kafka Configuration - non-sensitive settings
    KAFKA_BROKERS: "kafka.kafka.svc.cluster.local:9092"
    KAFKA_CONSUMER_TOPIC: "task.events"
    KAFKA_PRODUCER_TOPIC: "notification.events.v1"
    KAFKA_CONSUMER_GROUP: "notification-service"
    KAFKA_SASL_ENABLED: "true"
    KAFKA_SASL_MECHANISM: "SCRAM-SHA-256"
    KAFKA_SASL_USERNAME: "kafka-user"
    # User Service
    USER_SERVICE_ADDR: "user-service.dev.svc.cluster.local:50051"
    # Logging
    LOG_LEVEL: "debug"
    LOG_FORMAT: "console"

  # =========================================================
  # SENSITIVE ENVIRONMENT VARIABLES (Secrets)
  # =========================================================
  envVarsSecret:
    MONGODB_URI: "mongodb://root:MongoDB%40Root2024Secure%21@task-mongodb-mongodb-sharded.mongodb-task.svc.cluster.local:27017/notification_db?authSource=admin"
    REDIS_PASSWORD: "Redis@Cluster2024Secure!"
    KAFKA_SASL_PASSWORD: "Kafka@SASL2024Secure!"

  # =========================================================
  # CONTAINER PORTS
  # =========================================================
  containerPorts:
    http-metrics:
      port: 9093

  # =========================================================
  # SERVICE CONFIGURATION
  # =========================================================
  service:
    type: ClusterIP
    ports:
      http-metrics:
        port: 9093
        targetPort: http-metrics

  # =========================================================
  # RESOURCE REQUESTS & LIMITS
  # =========================================================
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 300m
      memory: 256Mi

  # =========================================================
  # HEALTH PROBES
  # =========================================================
  livenessProbe: null
  readinessProbe: null

  # =========================================================
  # MONITORING & ERROR TRACKING
  # =========================================================
  sentry:
    enabled: false
    dsn: ""
    environment: development
    tracesSampleRate: 1.0

  metrics:
    enabled: true
    port: http-metrics
    path: /metrics
    interval: 30s
    serviceMonitor:
      labels:
        release: monitoring

  extraVolumes: []
  extraVolumeMounts: []
