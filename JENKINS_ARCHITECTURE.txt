╔══════════════════════════════════════════════════════════════════════════════╗
║                     JENKINS CI/CD ARCHITECTURE                               ║
║                     Task Management System                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│  MINIKUBE / EKS CLUSTER                                                     │
│                                                                             │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │  JENKINS NAMESPACE                                                 │    │
│  │                                                                    │    │
│  │  ┌──────────────────────────────────────────────────────────┐     │    │
│  │  │  JENKINS MASTER POD                                      │     │    │
│  │  │  ┌────────────────────────────────────────────────┐     │     │    │
│  │  │  │  Jenkins Server Container                      │     │     │    │
│  │  │  │  - Web UI (Port 8080)                          │     │     │    │
│  │  │  │  - API Server                                  │     │     │    │
│  │  │  │  - Job Scheduler                               │     │     │    │
│  │  │  │  - Plugin Manager                              │     │     │    │
│  │  │  │                                                 │     │     │    │
│  │  │  │  Installed Plugins:                            │     │     │    │
│  │  │  │  ✓ Kubernetes                                  │     │     │    │
│  │  │  │  ✓ Workflow Aggregator (Pipelines)             │     │     │    │
│  │  │  │  ✓ Generic Webhook Trigger                     │     │     │    │
│  │  │  │  ✓ GitHub Integration                          │     │     │    │
│  │  │  │  ✓ Docker Workflow                             │     │     │    │
│  │  │  │  ✓ Git Client                                  │     │     │    │
│  │  │  └────────────────────────────────────────────────┘     │     │    │
│  │  │                                                          │     │    │
│  │  │  Resources (Minikube):                                  │     │    │
│  │  │  - CPU: 250m request / 1000m limit                      │     │    │
│  │  │  - Memory: 256Mi request / 1024Mi limit                 │     │    │
│  │  │                                                          │     │    │
│  │  │  Resources (EKS Production):                            │     │    │
│  │  │  - CPU: 1000m request / 4000m limit                     │     │    │
│  │  │  - Memory: 2048Mi request / 8192Mi limit                │     │    │
│  │  └──────────────────────────────────────────────────────────┘     │    │
│  │                                                                    │    │
│  │  ┌──────────────────────────────────────────────────────────┐     │    │
│  │  │  PERSISTENT VOLUME CLAIM                                 │     │    │
│  │  │  - Jenkins Home: /bitnami/jenkins/home                   │     │    │
│  │  │  - Size: 5Gi (Minikube) / 50Gi (EKS)                    │     │    │
│  │  │  - Storage Class: standard (Minikube) / gp3 (EKS)       │     │    │
│  │  │  - Access Mode: ReadWriteOnce                            │     │    │
│  │  └──────────────────────────────────────────────────────────┘     │    │
│  │                                                                    │    │
│  │  ┌──────────────────────────────────────────────────────────┐     │    │
│  │  │  SERVICE ACCOUNT: jenkins-agent                          │     │    │
│  │  │  - Used by Jenkins agents                                │     │    │
│  │  │  - RBAC permissions for deployments                      │     │    │
│  │  └──────────────────────────────────────────────────────────┘     │    │
│  │                                                                    │    │
│  │  ┌──────────────────────────────────────────────────────────┐     │    │
│  │  │  DYNAMIC JENKINS AGENTS (Kubernetes Pods)                │     │    │
│  │  │                                                           │     │    │
│  │  │  ┌────────────────┐  ┌────────────────┐  ┌────────────┐ │     │    │
│  │  │  │ Agent Pod 1    │  │ Agent Pod 2    │  │ Agent Pod N│ │     │    │
│  │  │  │ - Build jobs   │  │ - Deploy jobs  │  │ - Test jobs│ │     │    │
│  │  │  │ - JNLP agent   │  │ - kubectl      │  │ - Custom   │ │     │    │
│  │  │  └────────────────┘  └────────────────┘  └────────────┘ │     │    │
│  │  │                                                           │     │    │
│  │  │  Container Cap: 5 (Minikube) / 20 (EKS)                 │     │    │
│  │  │  Auto-scaling based on job demand                        │     │    │
│  │  └──────────────────────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘

╔══════════════════════════════════════════════════════════════════════════════╗
║                          TERRAFORM STRUCTURE                                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

terraform/
├── modules/jenkins/              ← Reusable module
│   ├── main.tf                  ← Core resources
│   ├── variables.tf             ← Input variables
│   ├── outputs.tf               ← Output values
│   ├── values.yaml.tpl          ← Helm values template
│   └── scripts/groovy/          ← Init scripts
│
└── environments/
    ├── minikube/                ← Dev environment
    │   ├── jenkins.tf           ← Module usage
    │   ├── providers.tf         ← Minikube providers
    │   ├── variables.tf         ← Env variables
    │   └── Makefile             ← Helper commands
    │
    └── eks/                     ← Prod environment (to be created)
        └── ...